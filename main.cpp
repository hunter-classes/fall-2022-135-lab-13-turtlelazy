#include <iostream>
#include "funcs.h"

int main(){
    std::cout << "printRange from -2 to 10: " << printRange(-2, 10) << "\n";
    std::cout << "sumRange from -2 to 10: " << sumRange(-2, 10) << "\n";
    return 0;
}